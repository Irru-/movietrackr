<% if signed_in? %>
  <h1>Hello, <%= current_user.username %>!</h1>
  Click <%= link_to "here", signout_path, method: "delete" %> to log out.
<% else %>
  Noob
<% end %>

<br />
<br />
<% if @movies.empty? %>
Geen films in database
<% end %>
<% @movies.each do |film| %>
<%= film.id %> - <%= film.title %> - <%= film.rating %> - <%= link_to "delete", film, method: :delete %><br />
<%end%>
<br />
<%= link_to 'Add Movie', new_movie_path %>