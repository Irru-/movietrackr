<% if signed_in? %>
  <h1>Hello, <%= current_user.username %>!</h1>
  Click <%= link_to "here", signout_path, method: "delete" %> to log out.
<% else %>
  Noob
<% end %>

<br />
<br />
<% if @movies.empty? %>
Geen films in database
<% end %>
<% @movies.each do |film| %>
<%= film.id %> - <%= film.title %> - <%= film.rating %> - <%= film.user.username %>  

<% if signed_in? && current_user.admin? %>

- <%= link_to "delete", film, method: :delete %>

<% end %>

<br />

<%end%>
<br />
<% if signed_in? %>
	<%= link_to 'Add Movie', new_movie_path %>
<% else %>
	<%= link_to 'Log in', signin_path %>
<% end %>